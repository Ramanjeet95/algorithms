package sorting;

import java.util.Arrays;

public class MergeSort
{
	public int[] sort(int[] array, int l, int r, int size)
	{
		if(array.length == 2)
		{
			
		}
		
		return sort(array, r, r+2, size/2);
	}
}
